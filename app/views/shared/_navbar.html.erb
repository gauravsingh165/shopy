<nav class="navbar navbar-expand-lg navbar-warning bg-warning ">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">FlipKart</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      </ul>
      <div class="flex justify-between items-center">
        <div class="mx-5">
          <%= form_with url: products_path, method: :get, data: { controller: "form", action: "input->form#submit", turbo_frame: "products", turbo_action: "advance" } do %>
            <%= text_field_tag "filter", nil, placeholder: "Filter", class: "inline-block rounded-md border border-gray-200 outline-none px-3 py-2 w-full" %>
          <% end %>
        </div>
      </div>
      <% if user_signed_in? %>
        <h5 style="color:green"><%= current_user.email %></h5>
        <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link btn btn-outline-info rounded-5 mx-2' %>
        <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: 'navbar-link btn btn-outline-danger rounded-5', data: { turbo_frame: 'navbar', turbo_method: :delete } %>
      <% else %>
        <%= link_to 'Sign up', new_user_registration_path, :class => 'navbar-link btn btn-outline-success rounded-2' %>
        <%= link_to 'Login', new_user_session_path, :class => 'navbar-link btn btn-success rounded-3' %>
      <% end %>
        <%= link_to "Cart", card_path(id: 1) , class: "btn btn-outline-primary rounded-5 mx-5" %> 
    </div>
  </div>
</nav>